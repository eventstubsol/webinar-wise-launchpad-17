import { CheckCircle } from "lucide-react";
const bonusFeatures = ["CSV upload fallback (for users without Zoom OAuth)", "Mobile-ready dashboards", "Real-time sync status tracking", "Custom branding on all reports"];
export const BonusFeaturesSection = () => {
  return;
};